# TODO

## Fixes

- [ ] Fix issue where when a user creates a second order, it overwrites the previous order for that user
- [ ] Labels should have check boxes auto checked based on if we marked it picked up in the Campaign Orders view
- [ ] Order # on the summary sheet doesn't match the order # on the label
- [ ] Title on the summary shee says Summary and Order Sheets - it's just the summary
- [ ] Digging through the SQL table schema, I see at least one column called plant_type. We should use a more generic name since this will be used for more than just plants/flower fundraisers.
## Enhancements

- [ ] Add a way to set the wholesale price for each product - use it for the supplier order summary and to calcluate and show the profit for the fundraiser
- [ ] Add order total on the Campaign Orders view
- [ ] Update the labels after reviewing with Melanie what we need to show. 